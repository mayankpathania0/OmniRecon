# OmniRecon
A fast, zero-dependency OSINT utility for passive subdomain discovery and active host verification. It extracts subdomain records directly from public TLS/SSL Certificate Transparency logs via crt.sh and utilizes concurrent thread sockets to map a target's live external infrastructure.
